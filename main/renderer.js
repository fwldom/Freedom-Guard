const { ipcRenderer } = require("electron");
var {links,process_warp,Download,connectWarp,connectVibe,connectAuto,connect,SetProxy,disconnectWarp} = require("./core")
const path = require('path');
var sect = "main-core"